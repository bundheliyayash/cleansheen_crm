# Cleansheen SEO Redirects

# RewriteEngine On
# RewriteBase /

# # Force HTTPS and non-www (Best practice to prevent duplicate content)
# RewriteCond %{HTTPS} off [OR]
# RewriteCond %{HTTP_HOST} ^www\. [NC]
# RewriteCond %{HTTP_HOST} ^(?:www\.)?(.+)$ [NC]
# RewriteRule ^ https://cleansheen.in%{REQUEST_URI} [L,NE,R=301]

# # Redirect non-existing URLs to homepage
# RewriteCond %{REQUEST_FILENAME} !-f
# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteRule . https://cleansheen.in/ [L,R=301]

# # Handle ErrorDocument 404 (Fallback)
# ErrorDocument 404 https://cleansheen.in/
